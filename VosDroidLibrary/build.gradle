apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile files('libs/android-midi-libv3-49.jar')
}
tasks.withType(Compile) {
    options.encoding = "UTF-8"
}