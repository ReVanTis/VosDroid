apply plugin: 'java'

sourceCompatibility = 1.7
targetCompatibility = 1.7
dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    runtime project(':android-midi-libv3-49')
}
tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}